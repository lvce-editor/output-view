import type { OutputState } from '../OutputState/OutputState.ts'

export const handleSelect = async (state: OutputState): Promise<OutputState> => {
  // TODO update output channel
  return {
    ...state,
  }
}
