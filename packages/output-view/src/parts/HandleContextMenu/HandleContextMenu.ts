import type { OutputState } from '../OutputState/OutputState.ts'

export const handleContextMenu = async (state: OutputState, x: number, y: number, button: number): Promise<OutputState> => {
  // TODO open context menu
  return state
}
